# Private MongoDB connection. Do not commit .env.
MONGODB_URI=
MONGODB_DB_NAME=markecia

# Must be a long, random secret unique to each environment.
JWT_SECRET=
DEMO_BUYER_PASSWORD=
DEMO_ADMIN_PASSWORD=

# Comma-separated browser origins allowed to call this API.
CORS_ORIGINS=http://localhost:5173
